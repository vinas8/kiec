{% extends 'base-login.html.twig' %}

{% block page_content %}
    {#todo: normalios formos, remember, password reset #}
    <body class="login-page" style="background: url(../images/bg.png)">
        <div class="login-box">
            {% include ':inc:logo.html.twig' %}

            <div class="login-box-body">
                <p class="login-box-msg">Prašome prisijungti</p>
                <form action="{{ path("fos_user_security_check") }}" method="post">
                    <div class="form-group has-feedback">
                        <input type="text" name="_username" class="form-control" placeholder="Elektroninis paštas"/>
                        <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
                    </div>
                    <div class="form-group has-feedback">
                        <input type="password" name="_password" class="form-control" placeholder="Slaptažodis"/>
                        <span class="glyphicon glyphicon-lock form-control-feedback"></span>
                    </div>
                    {% if error is defined and error %}
                        <div class="alert alert-danger alert-small">{{ error|trans }}</div>
                    {% endif %}
                    <div class="row">
                        <div class="col-xs-8">
                            <div class="checkbox">
                                <label>
                                    <input type="checkbox"> Prisiminti mane
                                </label>
                            </div>
                        </div><!-- /.col -->
                        <div class="col-xs-4">
                            <button type="submit" class="btn btn-primary btn-block btn-flat">Prisijungti</button>
                        </div><!-- /.col -->
                    </div>
                </form>

                <div class="social-auth-links text-center">
                    <p>Jūs čia pirmą kartą?</p>

                <a href="{{ path('fos_user_registration_register') }}" class="btn btn-block btn-flat btn-primary">Registruotis</a>
	 	<hr style="margin-top: 53px">	
		
                    {% for owner in hwi_oauth_resource_owners() %}
                        <a href="{{ hwi_oauth_login_url(owner) }}"
                           class="btn btn-block btn-social btn-facebook"><i class="fa fa-facebook"></i>
                            Prisijungti su Facebook</a>
                        <a href="{{ hwi_oauth_login_url(owner) }}"
                           class="btn btn-block btn-social btn-google"><i class="fa fa-google-plus"></i>
                            Prisijungti su Google+</a>
                    {% endfor %}
		 <a href="mailto:navickaszilvinas@gmail.com?Subject=Dėl mokinių fizinio aktyvumo stebėjimo programos">Klausimus, pasiūlymus, pastebėjimus rašykite el paštu navickaszilvinas@gmail.com</a>
                </div><!-- /.social-auth-links -->
            </div><!-- /.login-box-body -->
        </div>
    </body>

{% endblock page_content %}
